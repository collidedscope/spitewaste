import math ; pow

srand: ^$seed ret

rand:
  push $seed dup dup load
  push 48271 mul
  push 2,31 :pow $-- mod
  store load ret

rand_range: ; [a b]
  $++ copy 1 sub :rand swap mod add ret

dice: ^-2 push 1 :aryfill map (@-2 :rand_range) ret
